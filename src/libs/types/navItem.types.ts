export interface INavItem {
  /**
   * The label of the nav item
   * @example 'How it works'
   * @type {string}
   */
  label: string;
  /**
   * The URL the nav item links to
   * @example '/how-it-works'
   * @type {string}
   */
  href: string;
}
